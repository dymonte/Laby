image: gcc

stages:
  - build
  - test
  - clean

variables:
  MAKE: make

build:
  stage: build
  script:
    - ${MAKE} maze
  artifacts:
    paths:
      - obj
      - ./maze
    when: on_success

test:
  stage: test
  script:
    - ${MAKE} run

clean:
  stage: clean
  script:
    - ${MAKE} clean